import {StyleSheet} from "react-native";
import {CLR_DRK_BLUE} from "./colors";
export const inputDefault = StyleSheet.create({
  container: {
    margin: 15,
    height: 60,
    borderColor: CLR_DRK_BLUE,
    padding: 10,
    fontSize: 28,
    borderWidth: 1
  }
})
